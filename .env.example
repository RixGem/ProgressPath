# ==============================================================================
# ENVIRONMENT VARIABLE NAMING CONVENTIONS
# ==============================================================================
# This application supports MULTIPLE naming conventions for environment variables
# to maintain compatibility with various deployment platforms and legacy configurations.
#
# SUPPORTED CONVENTIONS:
# 1. Standard Next.js convention (NEXT_PUBLIC_* for client-side variables)
# 2. Legacy/Alternative naming (without NEXT_PUBLIC_ prefix)
#
# The application will automatically check for both naming patterns and use
# whichever is available, with preference given to the standard Next.js convention.
# ==============================================================================

# Supabase Configuration
# ----------------------
# STANDARD naming (recommended):
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# LEGACY/ALTERNATIVE naming (also supported):
# SUPABASE_URL=your_supabase_url_here
# SUPABASE_ANON_KEY=your_supabase_anon_key_here
# (Service role key has no alternative naming)
#
# Note: If both naming conventions are present, the standard NEXT_PUBLIC_* 
# variables will take precedence.

# OpenRouter API Configuration
# Get your API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here

# OpenRouter Model Selection
# Specifies which AI model to use for generating daily quotes
# Default uses Meta's Llama 3.1 8B Instruct (free tier)
# Other free options: mistralai/mistral-7b-instruct:free, google/gemma-7b-it:free
# See all models at: https://openrouter.ai/models
OPENROUTER_MODEL_ID=meta-llama/llama-3.1-8b-instruct:free

# Application URL
# ---------------
# STANDARD naming (recommended):
NEXT_PUBLIC_APP_URL=http://localhost:3000

# LEGACY/ALTERNATIVE naming (also supported):
# APP_URL=http://localhost:3000
#
# Fallback behavior: If neither is set, the application will attempt to
# construct the URL using Vercel environment variables or default to localhost.

# Cron Job Security
# Generate a secure random string for production
# You can use: openssl rand -base64 32
CRON_SECRET=your_secure_cron_secret_here

# Test Endpoint Security (optional)
# ----------------------------------
# If not set, will automatically fall back to CRON_SECRET
TEST_SECRET=your_secure_test_secret_here

# JWT Embed Token Security
# -------------------------
# Secret key for signing JWT embed tokens for Notion integration
# Generate using: openssl rand -base64 32
#
# FALLBACK BEHAVIOR: If not set, the application will automatically use
# SUPABASE_SERVICE_ROLE_KEY as a fallback to ensure JWT signing still works.
# However, it's recommended to set a dedicated JWT_EMBED_SECRET for better
# security separation.
JWT_EMBED_SECRET=your_secure_jwt_embed_secret_here
